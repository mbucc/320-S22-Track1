import React from 'react';
import Typography from '@mui/material/Typography';

function Logo(props) {
    return (
        <Typography variant="h2">
            CLOG MONITOR
        </Typography>
    );
}

export default Logo;